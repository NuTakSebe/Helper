import { SortGroupsAndItemsPipe } from './sort-groups-and-items.pipe';

describe('SortGroupsAndItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortGroupsAndItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
